<!-- ### Create the edit view

Create an edit view template in views/songs and build a form with named controls.
The purpose of this form is to edit/update existing songs.
These fields should have the songs current attributes set as their value so that they can be properly updated. -->

        <h1>
          Edit song
          <%= @song.title %>
        </h1>
    <form action="/songs/<%=@song.id%>" method="post">
    <input type="hidden" name="_method" value="PATCH">

    <div>
      <label for="song">Title</label>
      <input id="song" type="text" name="song[title]" value="<%= @song.title %>">
    </div>

    <div>
      <label for="artist">Artist</label>
      <input id="artist" type="text" name="song[artist]" value="<%= @song.artist %>">
    </div>

    <div>
      <label for="album">Album</label>
      <input id="album" type="text" name="song[album]" value="<%= @song.album %>">
    </div>

    <div>
      <label for="genre">Genre</label>
      <input id="genre" type="text" name="song[genre]" value="<%= @song.genre %>">
    </div>

    <div>
      <label for="length">Length</label>
      <input id="length" type="text" name="song[length]" value="<%= @song.length %>">
    </div>

    <input type="submit" value="Save Changes">
  </form>
